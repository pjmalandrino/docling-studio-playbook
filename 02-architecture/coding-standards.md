# Coding Standards — <!-- REPLACE: Project Name -->

## Language: <!-- REPLACE: e.g. Python -->

### Style

- Formatter: <!-- REPLACE: e.g. ruff format, prettier, google-java-format -->
- Linter: <!-- REPLACE: e.g. ruff check, eslint, spotbugs -->
- Config file: <!-- REPLACE: e.g. pyproject.toml, .eslintrc.js -->

### Naming conventions

| Element | Convention | Example |
|---------|-----------|---------|
| Files | <!-- REPLACE --> | <!-- REPLACE --> |
| Classes | <!-- REPLACE --> | <!-- REPLACE --> |
| Functions | <!-- REPLACE --> | <!-- REPLACE --> |
| Constants | <!-- REPLACE --> | <!-- REPLACE --> |
| Variables | <!-- REPLACE --> | <!-- REPLACE --> |

### Imports

<!-- REPLACE: Describe import ordering rules, grouping, etc. -->

### Error handling

<!-- REPLACE: Describe the project's approach to errors (exceptions, result types, error codes, etc.) -->

### Testing

- Framework: <!-- REPLACE: e.g. pytest, vitest, JUnit 5 -->
- Naming: <!-- REPLACE: e.g. test_should_return_404_when_not_found -->
- Location: <!-- REPLACE: e.g. tests/ mirrors src/ structure -->
- Minimum coverage: <!-- REPLACE: e.g. 80% -->

### Project-specific rules

<!-- REPLACE: Any additional rules specific to your project -->

## Enforcement

These standards are enforced by:

- [ ] Pre-commit hooks
- [ ] CI pipeline
- [ ] Code review checklist (see [03-git-workflow/code-review-checklist.md](../03-git-workflow/code-review-checklist.md))
